# Model provider (used identically by BOTH stacks)
OPENAI_API_KEY=sk-...
BENCH_MODEL=gpt-4o-mini

# RocketRide Cloud (or ws://localhost:5565 for on-prem)
ROCKETRIDE_URI=https://api.rocketride.ai
ROCKETRIDE_AUTH=your-api-token
